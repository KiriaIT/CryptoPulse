# Sprint 1 checklist — Project setup & components (20 pts)

One-week sprint. Scoring is binary: all requirements met → 20 points, any single miss → 0.

## Team requirements

- [x] Public GitHub repository with a meaningful name (`KiriaIT/CryptoTracker`).
- [x] `rollingscopes` added as collaborator with Read access — invite from **Settings → Collaborators**.
- [x] README has project name, 2–3 sentence description, and team list with GitHub links.
- [x] Project initialised via Angular CLI with `strict: true` in `tsconfig.json` (Angular 21, standalone API).

## Individual — KiriaIT

- [x] At least one custom component (not `AppComponent`) merged into `main`, using `input()` or `output()` — `StatCardComponent`, `WatchlistFormComponent`, `MarketOverviewComponent` all qualify.
- [x] Diary entry at `development-notes/KiriaIT/KiriaIT-sprint-1-2026-05-11.md`, merged into `main`.
- [x] Entry contains both a summary of Sprint 1 work and a plan for Sprint 2.

## Individual — OgOqro (historical; left before Sprint 2)

These items have to come from OgOqro's own GitHub account — co-author trailers do not count.

- [x] At least one custom component merged into `main` using `input()` or `output()` — `WatchlistChipComponent` (`input.required` + `output`) and `PriceTickerComponent` (`input`), merged via PR #45.
- [x] Diary entry at `development-notes/OgOqro/OgOqro-sprint-1-2026-05-14.md`, merged into `main` via PR #46.
- [x] Entry includes Sprint 1 summary + Sprint 2 plan.

## Individual — Freemason-12 (active second contributor)

Same Sprint 1 bar as above. Commits must show **Freemason-12** as author — OgOqro's merged work does not count toward Freemason's score.

- [X] At least one custom component in `main` using `input()` and/or `output()` — authored and merged from Freemason-12's account.
- [X] Diary entry at `development-notes/Freemason-12/Freemason-12-sprint-1-YYYY-MM-DD.md`, merged via PR (**no squash-merge** on diary commits).
- [X] Entry includes Sprint 1 summary + plan for Sprint 2, and describes ≥2 feature components personally built (see `PROJECT_TRACKER.md` diary rules).

Onboarding: [`development-notes/Freemason-12/README.md`](./development-notes/Freemason-12/README.md).

## Notes

- Diary file naming follows the convention `<username>-sprint-<N>-<YYYY-MM-DD>.md`. The course spec accepts variations but the parser is reliable when the filename is explicit.
- Project choice (CryptoTrade) is locked from Sprint 1 onward and recorded in the README. Changing project is not allowed.
